services:
  piano-triads:
    image: ghcr.io/guilpejon/piano-triads:latest
    container_name: piano-triads
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
      - HOST=0.0.0.0
